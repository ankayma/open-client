/target
**/target
node_modules/
/frontend/**/dist
.DS_Store

# Tauri mobile scaffolding — regenerated by `cargo tauri android/ios init`,
# not source. Kept out of the repo to avoid committing generated projects.
/gui/src-tauri/gen/

# Manual-QC screenshots — show real infra (VPS IP, node IDs, internal repo
# names). LOCAL only; never push to this public repo (leak-check). [T:A.1.4/P.6]
/docs/test-evidence/

# Cosign signing — PRIVATE key never leaves the machine / CI File-var.
# Only cosign.pub (public) is committed. [T:A.1.21]
cosign.key
*.key

# Agent dev instructions — kept LOCAL only (references internal blueprint/SSOT +
# closed control-plane structure). Untracked so internal data is not added to this
# public repo. Claude still reads it locally; absent from the public tip.
/CLAUDE.md

# Wintun driver DLL — Microsoft-signed, fetched from wintun.net at build time by
# packaging/windows/build-ankayma-windows.bat (not vendored, to avoid shipping a
# third-party binary blob in this repo). [T:workspace private §5]
/gui/src-tauri/wintun.dll

# macOS Developer ID provisioning profile — supplied at build time (CI secret
# APPLE_PROVISION_PROFILE_BASE64, decoded by release-macos.yml). Not a secret in
# the cryptographic sense (it carries only the PUBLIC Developer ID certificate),
# but it is team-specific signing material, so it stays out of this public repo.
# Required whenever bundle.macOS.entitlements is wired — see scripts/release-macos.sh
# preflight and docs/macos-associated-domains.md. [T:A.1.21]
/gui/src-tauri/macos/*.provisionprofile
