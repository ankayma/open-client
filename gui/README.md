# gui — Tauri 2 shell (Part D §D.3)

`src-tauri/` Rust backend embeds `agent-core`; UI from `../frontend/app-gui`.
Scaffold at milestone 1.1 via `cargo tauri init` (Tauri toolchain required), then add
`gui/src-tauri` to the workspace `members` list. Desktop + mobile (Win/macOS/Linux/iOS/Android).
