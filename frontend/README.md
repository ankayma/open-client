# frontend — web-tech UI (Part D §D.3)

One web-tech codebase, reused across UI surfaces (Tauri web-frontend reuse, Part D §D.3.2):
- `app-gui/`   → entry for Tauri shell (desktop + mobile)
- `app-admin/` → entry for standalone Web UI admin console (Part B §B.5.2)
- `shared/`    → shared components/state

Framework (React / Svelte / Vue): **TBD per team** (Part D §D.7). Scaffold at milestone 1.1.
